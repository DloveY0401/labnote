# 2026-07-02

## 修复: 小组件装饰小熊图片不显示
- **问题**: 小熊装饰图片导入和 `bearImages` 对象存在，但对应的 JSX 渲染代码在之前编辑中被误删
- **修复**: 在 `WidgetPage.tsx` 中恢复小熊渲染代码：absolute 定位在右下角，`pointer-events-none`，受 `prefs.bearEnabled/bearSize/bearOpacity/bearSet` 控制
- 构建: 零错误通过，已打包

## 优化: 小熊布局改为双熊分置两角 + 圆角裁剪 + 边缘柔化
- 左上角 (brown bear): `rounded-br-2xl` (右下圆角) + `mask-image: linear-gradient(to bottom right, black 30%, transparent 100%)` 向右下渐变消隐
- 右上角 (white bear): `rounded-bl-2xl` (左下圆角) + `mask-image: linear-gradient(to bottom left, black 30%, transparent 100%)` 向左下渐变消隐
- 均设置 `pointer-events-none` 不阻挡交互
- 构建: 零错误通过，已打包

## 修复: 小熊图片遮挡锁定/设置按钮
- 将小熊 `top-0` 改为 `top-8` (32px，header 高度)，使图片下移至 header 下方，不再遮挡按钮
- 构建: 零错误通过，已打包
