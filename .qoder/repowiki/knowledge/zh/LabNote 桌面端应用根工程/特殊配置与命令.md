开发：`npm run dev`（并行启动 Vite dev server 与 Electron，wait-on 等待 5173 端口）；构建：`npm run build`（先 vite build 再 tsc -p tsconfig.node.json）；打包：`npm run dist`（调用 electron-builder 生成 release/LabNote-Setup-*.exe）。