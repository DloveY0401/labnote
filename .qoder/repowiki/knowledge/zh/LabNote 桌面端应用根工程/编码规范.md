- TypeScript 双配置：tsconfig.json 管理前端源文件，tsconfig.node.json 管理 Electron 主进程，分别由 vite 与 tsc 独立编译。
- 路径别名统一使用 `@/` 映射到 src 目录，前后端均遵循此约定。
- 构建产物严格分离：渲染层输出 dist/，主进程输出 dist-electron/，electron-builder 按目录名打包，禁止混放。