React + TypeScript；Ketcher 通过静态 `./ketcher/index.html` 以 iframe 形式嵌入；自定义 Canvas 2D 渲染；SMILES 解析为手写状态机（支持 `[ ]` 原子、环编号 `%nn`、芳香小写元素、`/\` 立体键）；PubChem 命名查询走 PUG REST (`/rest/pug/compound/smiles/{smi}/cids/TXT` → `/description/JSON`)，优先读取 `window.labnote.compound` 本地 SQLite 缓存。